namespace CannedBytes.Midi.Device.Message
{
    /// <summary>
    /// The MessageExchangeManager class controls the message exchange flow
    /// based on predefined (configured) message exchange patterns.
    /// </summary>
    public class MessageExchangeManager
    {
        // load state table
        // hook to operations for sending a specific message
        // hook to receive message and decipher message
    }
}