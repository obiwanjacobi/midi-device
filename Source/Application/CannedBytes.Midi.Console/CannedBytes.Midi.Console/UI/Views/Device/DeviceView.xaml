<UserControl x:Class="CannedBytes.Midi.Console.UI.Views.Device.DeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
             xmlns:ctrl="clr-namespace:CannedBytes.Windows.Controls;assembly=CannedBytes.Windows"
             xmlns:metro="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ctrl:Panorama x:Name="Categories">

        </ctrl:Panorama>

        <metro:TransitioningContentControl Grid.Row="1" x:Name="ActiveValueGroup" Transition="UpTransition" />

    </Grid>
</UserControl>